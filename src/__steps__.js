/**
 * 1. create a firebase project
 * 2. create a web app
 * 3. nmp i firebase
 * 4. save firebase config and export app
 * 5. Build> Authentication > enable sign in method
 
 */


/**
 *  1. create a context provider after validing password in singUp
 * 2.  create a context and set provider
 * 3. set the children proops by receiving as a proops
 * 4. set context value
 * 5. set the provider in main.jsx
 * 
 */